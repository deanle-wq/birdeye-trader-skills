interface:
  display_name: "🐳 Large Token Trades"
  short_description: "Find the largest recent token trades"
  default_prompt: "Use $birdeye-large-trade-scanner to answer: What unusually large trades hit this token recently?"
